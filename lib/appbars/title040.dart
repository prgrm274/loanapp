import 'package:flutter/material.dart';

class Title040 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Masuk',
        style: TextStyle(
          fontSize: 22
        ),
      ),
    );
  }
}
